name: Sync Fork

on:
  schedule:
    - cron: '*/30 * * * *' # every 30 minutes
  workflow_dispatch: # on button click

jobs:
  sync:

    runs-on: ubuntu-latest

    steps:
      - uses: tgymnich/fork-sync@v2.0
        with:
          token: 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
          owner: alibaba
          base: master
          head: master
